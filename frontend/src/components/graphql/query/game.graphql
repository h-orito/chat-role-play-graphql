query Game($id: ID!) {
  game(id: $id) {
    id
    name
    status
    participants {
      id
      name
      entryNumber
      profileIcon {
        id
        url
      }
    }
    periods {
      id
    }
    settings {
      chara {
        canOriginalCharacter
      }
      capacity {
        min
        max
      }
      rule {
        canShorten
        canSendDirectMessage
      }
      time {
        periodPrefix
        periodSuffix
        periodIntervalSeconds
        openAt
        startParticipateAt
        startGameAt
      }
      password {
        hasPassword
      }
    }
  }
}
