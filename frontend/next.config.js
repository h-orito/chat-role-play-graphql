/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/chat-role-play',
    images: {
        domains: [
            'placehold.jp' // TODO: 後ほど削除
        ]
    }
}

module.exports = nextConfig
